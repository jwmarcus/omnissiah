"""Providers for {{ASSISTANT_NAME}}.

base.py defines the provider-agnostic interfaces (Protocols) for each
capability category. notes.py is a real, runnable example backed by a local
JSON file under var/. Real email/calendar/chat providers implement the
matching interface and get wired into the MCP server in mcp/server.py.
"""
